<?php

    // perform php specific actions here (i,e, auth)

?>

<% if (htmlWebpackPlugin.options.isCustomOption) { %><?php

    // an example how to switch behaviour based on some custom supplied option
    // via `html-webpack-plugin` using webpack

?><% } %>

<!doctype html>
<html lang="en">
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0">
    <title><%= htmlWebpackPlugin.options.title %></title>
</head>

<body>
    <!-- React hooks and renders into this div -->
    <div id="root"></div>
    <!-- HtmlWebpackPlugin automatically includes all assets (e.g., bundle.js, main.css) with correct HTML tags -->
    <% for (var css in htmlWebpackPlugin.files.css) { %><link href="<%= htmlWebpackPlugin.files.css[css] %>" rel="stylesheet"><% } %>
    <% for (var chunk in htmlWebpackPlugin.files.chunks) { %><script src="<%= htmlWebpackPlugin.files.chunks[chunk].entry %>"></script><% } %>
    <!-- Fonts and external assets -->
    <!--<link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">-->
</body>

</html>
